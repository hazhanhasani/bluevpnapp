# BlueVPN 3.0.1 — Live Operator & UI Fix

این نسخه موارد زیر را اصلاح می‌کند:

- تشخیص اپراتور از سیم‌کارت فعال اینترنت، نه سیم‌کارت پیش‌فرض تماس
- تشخیص شبکه فیزیکی زیر VPN برای Wi-Fi و اینترنت همراه
- ارسال Heartbeat فنی هر ۳۰ ثانیه برای داده زنده BlueAI
- جلوگیری از اثر Heartbeat روی امتیاز یادگیری مسیرها
- همگام‌سازی پس‌زمینه بدون پنجره قفل‌شونده و تست اجباری تمام سرورها
- حذف درخواست تکراری همگام‌سازی اشتراک
- صفحه اصلی ساده‌تر و متمرکز بر اتصال
- تب‌های واقعی پنل مدیریت؛ فقط یک بخش در هر لحظه نمایش داده می‌شود
- پیام‌های موفقیت قابل‌بستن و یک‌بارمصرف
- تازه‌سازی زنده آمار پنل هر ۸ ثانیه
