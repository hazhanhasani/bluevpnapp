# BlueVPN 3.0.59 — رفع خطای 500 پنل مدیریت

## علت
در نسخه 3.0.58 قالب پنل از `sms_provider_lines` و `sms_provider_patterns` استفاده می‌کرد، اما مسیر `/admin` این دو متغیر را پیش از رندر مقداردهی نکرده بود و `NameError` باعث پاسخ 500 می‌شد.

## اصلاحات
- مقداردهی کاتالوگ خطوط و پترن‌ها پیش از رندر پنل
- نمایش امن پنل حتی وقتی هنوز همگام‌سازی انجام نشده یا فهرست خالی است
- تست واقعی ورود مدیر و بازشدن `/admin` بدون خطای 500
- حفظ کامل همگام‌سازی خودکار فراز اس‌ام‌اس نسخه 3.0.58

- Version: 3.0.59
- Version Code: 30059
