name: BlueVPN One-Time 5.10.7 Release Kick

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/kick-release-51007.yml'

permissions:
  contents: write
  actions: write

jobs:
  kick:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact kickoff source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Verify requested version
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(python -c 'import json;print(json.load(open("version.json"))["version"])')"
          test "$VERSION" = "5.10.7"
          echo "Kicking coordinated BlueVPN $VERSION release from $GITHUB_SHA"

      - name: Dispatch all release targets
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_SHA: ${{ github.sha }}
        shell: bash
        run: |
          set -euo pipefail
          gh api --method POST "repos/${GITHUB_REPOSITORY}/dispatches" \
            -f event_type='bluevpn_build' \
            -F 'client_payload[ref]'='main' \
            -F 'client_payload[target_sha]'="$SOURCE_SHA" \
            -F 'client_payload[build_mode]'='full'

          gh workflow run build-windows.yml --repo "$GITHUB_REPOSITORY" --ref main \
            -f target_sha="$SOURCE_SHA"
          gh workflow run build-ios.yml --repo "$GITHUB_REPOSITORY" --ref main \
            -f target_sha="$SOURCE_SHA"
          gh workflow run bluevpn-site-theme-release.yml --repo "$GITHUB_REPOSITORY" --ref main \
            -f target_sha="$SOURCE_SHA"

      - name: Remove one-time kickoff workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "bluevpn-release-bot"
          git config user.email "bluevpn-release-bot@users.noreply.github.com"
          git pull --ff-only origin main
          rm -f .github/workflows/kick-release-51007.yml
          git add .github/workflows/kick-release-51007.yml
          git commit -m "chore(ci): remove one-time 5.10.7 release kick [skip ci]"
          git push origin HEAD:main
