# انتشار BlueVPN 3.0.41

## رفع خطای «مشکل در تجزیه بسته» هنگام بروزرسانی داخلی

- APK پس از دانلود دیگر مستقیماً از URI حافظه خارجی به نصب‌کننده تحویل داده نمی‌شود.
- فایل در حافظه خصوصی برنامه ذخیره و بایت‌به‌بایت داخل `PackageInstaller.Session` اندروید نوشته می‌شود.
- قبل از نصب، SHA-256، اندازه، packageName، versionName، versionCode و امضای دیجیتال بررسی می‌شوند.
- نتیجه واقعی نصب شامل ناسازگاری، کمبود فضا، اختلاف امضا یا لغو کاربر با پیام روشن مدیریت می‌شود.
- برای دستگاه‌های OEM ناسازگار، مسیر FileProvider اختصاصی فقط به‌عنوان fallback باقی مانده است.
