# فعال‌سازی خودکار اینماد بلوپنل — نسخه 3.0.34

نشان رسمی ارائه‌شده برای دامنه `bot.blluepanel.ir` مستقیماً در پروژه تنظیم شده است. پس از Deploy نسخه 3.0.34، نشان در صفحه اصلی و بخش «اعتماد و شفافیت» بدون هیچ متغیر Railway نمایش داده می‌شود.

## اطلاعات ثبت‌شده

- شناسه اینماد: `748781`
- کد عمومی نشان: `HuvEauyphrDRR17dhwoisDFNoMFMkDC0`
- صفحه بررسی اعتبار: `https://trustseal.enamad.ir/?id=748781&Code=HuvEauyphrDRR17dhwoisDFNoMFMkDC0`
- تصویر نشان: `https://trustseal.enamad.ir/logo.aspx?id=748781&Code=HuvEauyphrDRR17dhwoisDFNoMFMkDC0`

## تنظیمات Railway

برای نمایش نشان، هیچ متغیری لازم نیست. متغیرهای `ENAMAD_ID`، `ENAMAD_CODE`، `ENAMAD_VERIFY_URL` و `ENAMAD_LOGO_URL` فقط برای جایگزینی نشان در آینده هستند و می‌توانند خالی بمانند.

## مسیر نمایش

`https://bot.blluepanel.ir/`

کد HTML خروجی شامل `referrerpolicy="origin"` روی لینک و تصویر و همچنین ویژگی `code` رسمی روی تصویر است.
