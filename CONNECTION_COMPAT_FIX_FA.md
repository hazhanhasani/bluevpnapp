# اصلاح سازگاری اتصال BlueVPN با v2rayNG

این اصلاح برای حالتی است که یک کانفیگ در v2rayNG کار می‌کند اما BlueVPN آن را خیلی زود «نامعتبر/در دسترس نیست» تشخیص می‌دهد.

## علت اصلی

BlueVPN چند لایه بررسی اضافی قبل از اجرای واقعی Xray داشت که از رفتار upstream v2rayNG سخت‌گیرانه‌تر بودند:

- شکست DNS سیستم اندروید به‌عنوان شکست قطعی کانفیگ در نظر گرفته می‌شد.
- شکست TCP خام روی endpoint به‌عنوان شکست قطعی در نظر گرفته می‌شد؛ این تست برای همه transportها معادل handshake واقعی Xray نیست.
- فقط دو IP اول DNS تست می‌شدند و در بعضی شبکه‌ها ممکن بود هر دو IPv6 نامناسب باشند در حالی که IPv4 سالم وجود دارد.
- timeout راه‌اندازی Core حدود 2.1 ثانیه بود و می‌توانست قبل از آماده‌شدن local proxy یک کانفیگ سالم را حذف کند.
- تست اینترنت فقط حدود 1.05 ثانیه فرصت داشت.
- BlueVPN یک فیلتر سفارشی TLS روی import اضافه کرده بود که می‌توانست profile را قبل از رسیدن به parser/core اصلی v2rayNG حذف کند.

## تغییرات

- Preflight اولیه فقط خطاهای قطعی محلی مثل host خالی و port نامعتبر را رد می‌کند.
- خطای DNS یا TCP خام دیگر به‌تنهایی باعث حذف کانفیگ نمی‌شود و تصمیم نهایی با Xray است.
- در DNS، IPv4 و IPv6 هر دو در نظر گرفته می‌شوند و IPv4 روی شبکه‌های موبایل اولویت دارد.
- فیلتر سفارشی TLS در BlueVPN حذف شد تا parsing/normalization با upstream v2rayNG هم‌راستا بماند.
- بعد از RUNNING شدن Core، timeout اولیه لغو می‌شود تا با تست واقعی تونل race نکند.
- قبل از تست اینترنت، آماده‌شدن local HTTP proxy تا 2.4 ثانیه بررسی می‌شود.
- پنجره تست واقعی اینترنت از حدود 1.05 ثانیه به 3.6 ثانیه افزایش یافت.
- timeout هر درخواست probe از 650ms به 1800ms افزایش یافت.
- timeout شروع Core از 2.1 ثانیه به 5.5 ثانیه افزایش یافت.
- یک کانفیگ فقط بعد از شکست واقعی end-to-end در همان چرخه اتصال quarantine می‌شود.
- با شروع تلاش جدید، quarantine موقت پاک می‌شود و کانفیگ دوباره فرصت تست دارد.

## اعتبارسنجی

- Pytest: 334 passed
- Generated Android validation: passed
- Dual-engine validation: 35 passed / 0 failed
- Build کامل Gradle/NDK/Go در محیط آفلاین اجرا نشد و طبق معماری پروژه در GitHub Actions انجام می‌شود.
