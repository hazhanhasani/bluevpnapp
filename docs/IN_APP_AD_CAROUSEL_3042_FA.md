# تبلیغات اسلایدی BlueVPN 3.0.42

تبلیغات از تب «تبلیغات» پنل مدیریت می‌شوند. تصویر می‌تواند روی فضای داده Railway آپلود شود یا از URL امن HTTP(S) دریافت شود. اپ تنظیمات را از `/api/v1/mobile/config` می‌گیرد، تصاویر را کش می‌کند و اسلایدها را با فاصله تعیین‌شده پخش می‌کند.
