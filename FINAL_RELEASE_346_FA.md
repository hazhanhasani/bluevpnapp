# BlueVPN 3.0.46 — رفع خطای Method Not Allowed هنگام رفرش پنل

## مشکل
در بعضی مرورگرهای موبایل، بعد از ثبت یا ویرایش یک فرم مدیریتی، آدرس مسیر POST در نوار مرورگر باقی می‌ماند. رفرش همان آدرس با متد GET باعث نمایش پاسخ خام زیر می‌شد:

`{"detail":"Method Not Allowed"}`

## اصلاحات
- بازیابی خودکار تمام مسیرهای مدیریتی POST هنگام بازشدن با GET یا HEAD
- انتقال امن کاربر به `/admin?recovered=1`
- مستثناکردن APIهای خواندنی پنل و صفحه ورود
- افزودن هدرهای `Cache-Control: no-store, private` و `Pragma: no-cache` برای صفحات پنل
- نمایش پیام فارسی «صفحه مدیریت پس از رفرش بازیابی شد» به‌جای JSON خام
- تست رگرسیون اختصاصی برای جلوگیری از بازگشت خطای 405

## نسخه
- Version: 3.0.46
- Version Code: 30046
- Tests: 144 passed
