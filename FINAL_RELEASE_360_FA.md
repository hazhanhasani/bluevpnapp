# BlueVPN 3.0.60 — دریافت همه پترن‌های فراز اس‌ام‌اس

## مشکل رفع‌شده
API فراز اس‌ام‌اس فهرست پترن‌ها را به‌صورت صفحه‌بندی‌شده و با اندازه پیش‌فرض ۱۵ مورد برمی‌گرداند. نسخه قبلی فقط صفحه اول را ذخیره می‌کرد.

## اصلاحات
- دریافت خودکار همه صفحات `/patterns`
- درخواست `per_page=100` و ادامه براساس `current_page`/`last_page`، `total` و لینک صفحه بعد
- پشتیبانی از پاسخ‌های بدون metadata با ادامه صفحه‌ها تا صفحه خالی
- حذف UIDهای تکراری
- توقف ایمن اگر API پارامتر `page` را نادیده بگیرد
- سقف ایمنی ۱۰۰ صفحه برای جلوگیری از حلقه بی‌نهایت

## نسخه
- Version: 3.0.60
- Version Code: 30060
