# BlueVPN 2 — نسخه‌گذاری پویا

## نسخه پایه پروژه

تمام Metadata داخلی پروژه روی نسخه پایه زیر همگام شده است:

- Project Version: `2.0.0`
- Base Version Code: `20000`

این نسخه پایه فقط برای سورس پروژه است و Release کاربران با آن منتشر نمی‌شود.

## نسخه Release

Workflow آخرین GitHub Release را می‌خواند:

- اگر آخرین نسخه هنوز از سری ۱ باشد، نسخه بعدی `2.0.1` می‌شود.
- اگر آخرین نسخه `2.0.1` باشد، نسخه بعدی `2.0.2` می‌شود.
- اگر آخرین نسخه `2.0.2` باشد، نسخه بعدی `2.0.3` می‌شود.

Version Code نیز به‌ترتیب افزایش می‌یابد:

- `2.0.1` → `20001`
- `2.0.2` → `20002`
- `2.0.3` → `20003`

نسخه به شماره Run گیت‌هاب وابسته نیست و ثابت هم نمی‌ماند. فقط پس از انتشار
موفق GitHub Release، شماره بعدی محاسبه می‌شود.

Build ID بسته: 20260804163403
