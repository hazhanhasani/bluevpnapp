# BlueVPN 3.0.81 — Selection Ownership & Auto Rotation

## مشکل رفع‌شده

در نسخه‌های قبل `beginSmartConnection()` قبل از اتصال، حالت انتخاب را دوباره روی Auto قرار می‌داد. بنابراین حتی اگر کاربر Premium یک سرور را به‌صورت دستی انتخاب می‌کرد، هنگام اتصال انتخاب او از دست می‌رفت. همچنین مسیر دستی دوباره Rank می‌شد و ممکن بود سرور دیگری از همان لوکیشن انتخاب شود. Auto نیز به علت رتبه‌بندی کاملاً قطعی معمولاً روی یک GUID ثابت باقی می‌ماند.

## معماری جدید انتخاب سرور

سه مالک مستقل برای انتخاب تعریف شد:

- `AUTO`: تمام Pool مجاز همان پلن را می‌بیند و انتخاب هوشمند انجام می‌دهد.
- `MANUAL_LOCATION`: فقط سرورهای همان کشور/لوکیشن را می‌بیند و Failover نیز از آن خارج نمی‌شود.
- `MANUAL_SERVER`: فقط همان GUID انتخاب‌شده را اجرا می‌کند؛ AI، Auto، Startup Optimization و Sync حساب حق تغییر آن را ندارند.

پلن رایگان همچنان فقط Auto است و امکان نشت حالت Manual از Premium به Free وجود ندارد.

## Auto Rotation

Auto همچنان سرورها را با پینگ، موفقیت اخیر، سلامت نشست، سابقه دستگاه و سیگنال BlueAI رتبه‌بندی می‌کند. اگر یک مسیر اختلاف کیفیت واضح داشته باشد همان مسیر انتخاب می‌شود؛ اما اگر چند مسیر در بازه کیفیت نزدیک باشند، اتصال‌های جدید بین آن‌ها به‌صورت کنترل‌شده می‌چرخند تا برنامه روی یک سرور ثابت قفل نشود.

## محافظت در برابر Override

- شروع اتصال دیگر `smartBalance=true` را به‌صورت اجباری اجرا نمی‌کند.
- Refresh وضعیت اشتراک، انتخاب دستی معتبر Premium را حفظ می‌کند.
- Startup optimizer سرور دستی دقیق را بازنویسی نمی‌کند.
- `orderedCandidates` و `fastCandidates` نیز Selection Mode را رعایت می‌کنند.
- انتخاب دقیق سرور در صفحه مکان‌ها به‌صورت اتمیک Mode + Location + GUID را ذخیره می‌کند.

## اعتبارسنجی

- 321 تست موفق، 0 ناموفق
- 35 بررسی معماری موفق
- Generated Android validation موفق
- Version: 3.0.81 / 30081
- Build کامل Gradle/NDK/Go در محیط آفلاین اجرا نشده و باید در GitHub Actions تأیید شود.
