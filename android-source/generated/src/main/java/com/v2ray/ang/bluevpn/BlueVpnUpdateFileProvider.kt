package com.v2ray.ang.bluevpn

import androidx.core.content.FileProvider

/** FileProvider subclass used only as a last-resort OEM installer fallback. */
class BlueVpnUpdateFileProvider : FileProvider()
