# BlueVPN 3.0.70 — بازطراحی سرعت و پایداری اجرا

- صفحه اصلی بدون انتظار همگام‌سازی نمایش داده می‌شود.
- آماده‌سازی Asset، حساب، تبلیغات و فهرست کامل سرورها از مسیر لمس کاربر جدا شد.
- اتصال رایگان ابتدا از سرورهای نصب‌شده محلی استفاده می‌کند و هنگام اتصال ساب را اجباری تازه نمی‌کند.
- کانفیگ‌های دارای allowInsecure/insecure=1 قبل از ذخیره و پیش از اجرای Core کنار گذاشته می‌شوند.
- پس‌زمینه تمام‌صفحه دیگر انیمیشن دائمی و مصرف GPU ندارد.
- اسکن کامل سرورها فقط در زمان بیکاری واقعی انجام می‌شود.
