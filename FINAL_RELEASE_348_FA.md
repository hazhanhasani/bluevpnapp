# BlueVPN 3.0.48 — رفع قطعی کادر بزرگ و خالی تبلیغات

## علت اصلی
نسخه 3.0.42 تبلیغ را قبل از دانلود موفق تصویر Visible می‌کرد و چون ارتفاع View روی WRAP_CONTENT بود، در برخی دستگاه‌ها کل فضای باقی‌مانده صفحه را می‌گرفت. در نتیجه فقط پس‌زمینه تیره و برچسب «تبلیغ» دیده می‌شد.

## اصلاحات
- تبلیغ در HomeActivity با ارتفاع اولیه صفر ساخته می‌شود.
- View تبلیغ تا زمان دانلود و Decode موفق Bitmap کاملاً GONE و صفرارتفاع باقی می‌ماند.
- پس از آماده‌شدن تصویر، ارتفاع دقیق نسبت ۲۰:۹ و بین ۱۱۶ تا ۱۶۰dp اعمال می‌شود.
- تبلیغ بدون تصویر معتبر وارد Carousel نمی‌شود.
- تصویر خراب یا فایل حذف‌شده باعث حذف خودکار همان اسلاید می‌شود.
- مسیر محلی تصویر قبل از URL کامل استفاده می‌شود تا دامنه اشتباه Railway مانع نمایش نشود.
- سرور نسخه‌های 3.0.47 و پایین‌تر را شناسایی کرده و تبلیغات را برای آن‌ها غیرفعال می‌کند تا صفحه اصلی خراب نشود.
- از نسخه 3.0.48 به بعد تبلیغات فعال و ایمن تحویل داده می‌شوند.

## نسخه
- Version Name: 3.0.48
- Version Code: 30048
- Tests: 154 passed
