{
  "name": "BlueVPN",
  "version": "1.0.4",
  "channel": "healthcheck-telegram-decoupling",
  "description": "Start FastAPI/Uvicorn before Telegram polling so Railway healthchecks can pass during zero-downtime deployments. Telegram startup and polling now run independently with automatic retries while the old replica drains."
}
