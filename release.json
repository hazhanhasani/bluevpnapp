{
  "name": "BlueVPN",
  "version": "1.0.3",
  "channel": "database-autopilot",
  "description": "Persistent PostgreSQL enforcement on Railway, automatic database and table creation, automatic schema migration, legacy column reconciliation, optional SQLite-to-PostgreSQL import, and fail-fast deployment behavior."
}
