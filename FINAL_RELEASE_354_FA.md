# BlueVPN 3.0.54 — مقاوم‌سازی خطای 502 فراز اس‌ام‌اس

## مشکل رفع‌شده
وقتی سرویس edge.ippanel.com پاسخ Cloudflare 502 و HTML برمی‌گرداند، متن خام HTML داخل صفحه حساب BlueVPN نمایش داده می‌شد و بخش بالای فرم را پر می‌کرد.

## اصلاحات
- عدم نمایش HTML، Cloudflare، CDN و جزئیات فنی سرویس پیامک به کاربر
- سه تلاش کنترل‌شده برای خطاهای موقت 408، 425، 429، 500، 502، 503 و 504
- پیام فارسی کوتاه پس از تمام‌شدن تلاش‌ها
- پاسخ API با کد قابل‌شناسایی `SMS_PROVIDER_TEMPORARY_UNAVAILABLE`
- هدر Retry-After برای خطاهای موقت
- محدودشدن فضای پیام وضعیت صفحه حساب به سه خط
- پردازش امن پاسخ‌های HTML یا JSON نامعتبر داخل اپ
- ثبت وضعیت فنی فقط در لاگ سرور، بدون افشای پاسخ کامل ارائه‌دهنده

## نسخه
- Version: 3.0.54
- Version Code: 30054
- Tests: 179 passed
