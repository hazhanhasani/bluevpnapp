{
  "name": "BlueVPN",
  "version": "0.4.7",
  "channel": "real-tunnel-verification",
  "description": "Only reports connected after an HTTP request succeeds through the local Xray proxy, forces VPN/local-proxy mode, and resets connection duration for every newly verified session."
}
