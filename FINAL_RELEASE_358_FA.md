# BlueVPN 3.0.58 — همگام‌سازی کامل فراز اس‌ام‌اس

## اصلاحات
- دریافت خودکار خطوط مجاز از `GET /ws/v1/lines/accessible`
- دریافت خودکار پترن‌های فعال از `GET /ws/v1/patterns`
- حذف ورود دستی UID پترن و شماره خط
- انتخاب خودکار خط اشتراکی معتبر حساب
- نمایش خطوط و پترن‌ها به‌صورت فهرست انتخابی داخل پنل
- تطبیق خودکار پترن‌های بدون ابهام با پیام‌های BlueVPN
- جلوگیری از ذخیره خط یا پترنی که متعلق به API Key فعلی نیست
- کنترل سازگاری متغیرهای پترن با متغیرهای پیام BlueVPN
- اعتبارسنجی زنده خط و پترن پیش از ارسال تست
- حفظ مسیر رسمی `POST /ws/v1/sms/pattern`

## نسخه
- Version: 3.0.58
- Version Code: 30058
- Tests: 199 passed
