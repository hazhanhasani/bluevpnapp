# انتشار نهایی BlueVPN / BluePanel 3.0.34

این نسخه نشان رسمی اینماد دامنه `bot.blluepanel.ir` را بدون نیاز به تنظیم دستی Railway فعال می‌کند.

## نشان ثبت‌شده

- شناسه: `748781`
- کد عمومی: `HuvEauyphrDRR17dhwoisDFNoMFMkDC0`
- لینک بررسی و تصویر نشان به‌صورت خودکار در زمان اجرای صفحه ساخته می‌شوند.
- ویژگی `referrerpolicy="origin"` و ویژگی رسمی `code` روی خروجی HTML قرار گرفته‌اند.

## نصب

فایل کامل را جایگزین پروژه قبلی کن یا فایل Hotfix را روی نسخه کامل 3.0.33 اعمال کن. بعد از Deploy، صفحه زیر نشان را نمایش می‌دهد:

`https://bot.blluepanel.ir/`

هیچ متغیر Railway برای اینماد لازم نیست. متغیرهای اینماد فقط Override اختیاری هستند.
