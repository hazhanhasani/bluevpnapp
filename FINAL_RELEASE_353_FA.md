# BlueVPN 3.0.53 — اصلاح تنظیمات فراز اس‌ام‌اس

## تغییرات
- افزودن حالت «خط اشتراکی فراز اس‌ام‌اس» به‌عنوان حالت پیش‌فرض
- حذف اجبار واردکردن شماره فرستنده برای حساب‌های دارای خط اشتراکی
- استفاده خودکار از `FARAZSMS_SHARED_FROM_NUMBER` و مقدار پیش‌فرض `+983000505`
- حفظ امکان واردکردن خط اختصاصی فقط در صورت انتخاب حالت اختصاصی
- حذف فیلد تکراری کد پترن ورود از تنظیمات اصلی پیامک
- حذف فیلد تکراری نام پارامتر ورود؛ قرارداد ورود به‌صورت ثابت `%code%` است
- خواندن پترن ورود فقط از بخش «پترن‌های پیامکی» و ردیف `auth_otp`
- حفظ mirror داخلی پترن برای سازگاری با نسخه‌های قبلی
- بهبود رابط موبایل و راهنمای مستقیم رفتن به بخش پترن‌ها

## نسخه
- Version: 3.0.53
- Version Code: 30053
- Tests: 173 passed
