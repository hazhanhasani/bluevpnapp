# BlueVPN 3.0.51 — رفع ناسازگاری مسیر تصویر تبلیغات دیتابیسی

## علت
تصاویر جدید داخل PostgreSQL با مسیر `/api/v1/ad-assets/...` ذخیره می‌شدند، اما اپ مسیر نسبی جدید را رد می‌کرد و چون `image_path` بر لینک کامل اولویت داشت، تبلیغ از لیست حذف می‌شد.

## اصلاح
- سرور برای تصاویر دیتابیسی فقط URL کامل HTTPS را به کلاینت‌های قدیمی می‌دهد.
- `image_path` فقط برای مسیر قدیمی `/media/ads/` ارسال می‌شود.
- اپ جدید مسیر `/api/v1/ad-assets/` را نیز می‌پذیرد.
- در صورت نامعتبر بودن مسیر نسبی، اپ به URL کامل تصویر برمی‌گردد.
- اپ 3.0.49 پس از Deploy بک‌اند جدید نیز می‌تواند تبلیغ را دریافت کند.
